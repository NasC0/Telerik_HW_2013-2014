The main purpose of SQL is to expose an interface for easy data manipulation in the database management system. A set of instructions are passed in as pure SQL, which is then translated to relational algebra equations, which in turn execute the desired operations over the stored data.